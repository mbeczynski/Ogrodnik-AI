apiVersion: v1
kind: Service
metadata:
  name: {{ include "ogrodnik.fullname" . }}
  labels:
    {{- include "ogrodnik.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: 8000
      protocol: TCP
      name: http
  selector:
    {{- include "ogrodnik.selectorLabels" . | nindent 4 }}
